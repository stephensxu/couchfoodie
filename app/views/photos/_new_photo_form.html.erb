<%= form_for([kitchen, photo], :multipart => true) do |f| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :picture %>
  </div>
  <div class="buttons">
    <%= f.submit 'Create photo', :class => "button_normal" %>
  </div>
<% end %>
